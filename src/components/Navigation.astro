---
const opciones = [
  { text: 'Productos', href: '/productos' },
  { text: 'Quienes somos', href: '/nosotros' },
]
---

<nav
  class="md:flex hidden justify-center items-center gap-3 px-5 font-medium text-stone-500"
>
  {
    opciones.map((op) => (
      <a
        class="hover:text-stone-800 hover:underline hover:underline-offset-4"
        href={op.href}
      >
        {op.text}
      </a>
    ))
  }
</nav>
